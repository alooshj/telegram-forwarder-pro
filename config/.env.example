# Telegram API credentials (get from https://my.telegram.org)
API_ID=your_api_id_here
API_HASH=your_api_hash_here

# Session string (generate via src/utils/session_generator.py)
SESSION_STRING=

# MongoDB Atlas connection (free tier M0)
# Use MONGODB_URI (standard Atlas format) or MONGO_URI
MONGODB_URI=mongodb+srv://username:password@cluster0.mongodb.net/telegram_forwarder?retryWrites=true&w=majority

# Database name
MONGO_DB=telegram_forwarder

# Web dashboard settings
WEB_HOST=0.0.0.0
WEB_PORT=5000
SECRET_KEY=change-me-in-production

# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Forwarding behavior
CHECK_INTERVAL=30
MAX_RETRIES=3
RETRY_DELAY=10
