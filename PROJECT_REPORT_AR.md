# 📊 التقرير الشامل والمتكامل لمشروع TeleTips Pro
**تاريخ التقرير:** 28 أغسطس 2026  
**اسم المنصة:** **TeleTips Pro**  
**المطور:** Google DeepMind / Antigravity AI  
**المستودع الرئيسي:** `alooshpal@gmail.com / telegram-forwarder-pro`  
**بيئة الاستضافة والتشغيل:** Render Cloud Platform (Python 3.11 / Web Service)

---

## 📑 فهرس التقرير
1. [مقدمة ونظرة عامة على النظام](#1-مقدمة-ونظرة-عامة-على-النظام)
2. [البنية الهندسية للبرنامج (Architecture & Tech Stack)](#2-البنية-الهندسية-للبرنامج-architecture--tech-stack)
3. [الأدوات، الخوادم، والمكتبات المستخدمة](#3-الأدوات-الخوادم-والمكتبات-المستخدمة)
4. [المحركات والأنظمة الفرعية الأساسية](#4-المحركات-والأنظمة-الفرعية-الأساسية)
5. [سجل المشاكل والتحديات التي تمت مواجهتها وكيف تم حلها](#5-سجل-المشاكل-والتحديات-التي-تمت-مواجهتها-وكيف-تم-حلها)
6. [النقاط المفتوحة والمتطلبات التشغيلية (ما يحتاج تهيئة من طرف المستخدم)](#6-النقاط-المفتوحة-والمتطلبات-التشغيلية)
7. [هيكل قواعد البيانات ونموذج البيانات (Data Models)](#7-هيكل-قواعد-البيانات-ونموذج-البيانات)
8. [الأمان والتشفير وإدارة الصلاحيات (Security & RBAC)](#8-الأمان-والتشفير-وإدارة-الصلاحيات)
9. [خاتمة وتوصيات التطوير المستقبلي](#9-خاتمة-وتوصيات-التطوير-المستقبلي)

---

## 1. مقدمة ونظرة عامة على النظام

منصة **TeleTips Pro** هي نظام سحابي متكامل ومتقدم (Multi-Tenant SaaS) مخصص لإدارة، أتمتة، تحويل، وتوجيه رسائل وقنوات تليجرام (Telegram Channels & Groups) في الوقت الفعلي بسرعة فائقة، مع دعم كامل للوسائط المتعددة (الألبومات، مقاطع الفيديو، المستندات، الرسائل الصوتية)، وفلترة الكلمات المفتاحية، واستبدال النصوص والروابط بنظام التعابير النمطية (Regex)، مع نظام فوترة متكامل بالعملات المشفرة وإدارة المشتركين.

---

## 2. البنية الهندسية للبرنامج (Architecture & Tech Stack)

تم بناء التطبيق باتباع معمارية الطبقات المنفصلة (Modular Multi-Layer SaaS Architecture):

```
                               ┌──────────────────────────────────────────────┐
                               │             TeleTips Pro Dashboard           │
                               │  (Tailwind CSS + Glassmorphism SPA Web UI)   │
                               └──────────────────────┬───────────────────────┘
                                                      │ HTTP / REST API & Cookies
                               ┌──────────────────────▼───────────────────────┐
                               │           Flask Web & API Server             │
                               │       (src/web/api.py, auth.py)              │
                               └──────┬──────────────────────┬────────────────┘
                                      │                      │
                 ┌────────────────────┴────────┐   ┌─────────┴─────────────────────┐
                 │  Auth & Encryption Manager  │   │  Billing & Webhook Controller │
                 │  (AES-256 Fernet + JWT)     │   │  (NOWPayments IPN & Licenses) │
                 └─────────────────────────────┘   └───────────────────────────────┘
                                      │                      │
     ┌────────────────────────────────┴──────────────────────┴─────────────────────────────────┐
     │                                    Core Worker Pool                                     │
     │                      (src/forwarder/worker_pool.py & engine.py)                         │
     │                                                                                         │
     │   ┌─────────────────────┐   ┌──────────────────────────┐   ┌────────────────────────┐   │
     │   │ Telethon MTProto    │   │  Rules & Regex Pipeline  │   │ Album / Media Group    │   │
     │   │ Multi-Client Client │   │  (src/rules/engine.py)   │   │ Collector (Debounced)  │   │
     │   └─────────────────────┘   └──────────────────────────┘   └────────────────────────┘   │
     └────────────────────────────────────────┬────────────────────────────────────────────────┘
                                              │
                    ┌─────────────────────────┴─────────────────────────┐
                    │                                                   │
        ┌───────────▼─────────────┐                         ┌───────────▼─────────────┐
        │  MongoDB Atlas (Cloud)  │◄──(Transparent Fallback)┤  SQLite Embedded DB     │
        │  Primary SaaS Database  │                         │  (Development / Local)  │
        └─────────────────────────┘                         └─────────────────────────┘
```

---

## 3. الأدوات، الخوادم، والمكتبات المستخدمة

### أ. خوادم الويب وبيئة التشغيل (Infrastructure & Runtime)
- **Python 3.11+**: لغة البرمجة الأساسية مع دعم كامل للـ Asyncio والتعدد الخيطي (Threading).
- **Gunicorn / Waitress**: خوادم تشغيل WSGI للإنتاج السحابي على Render.
- **Render Cloud Service**: منصة استضافة السيرفر مع خيار التشغيل التلقائي عبر `Procfile` و `render.yaml`.
- **Docker**: حاوية معزولة للنشر السريع في أي بيئة سحابية (`Dockerfile`).

### ب. المكتبات والأطر البرمجية (Core Libraries & Frameworks)
- **Flask & Flask-CORS**: لبناء واجهات الـ REST API وتوجيه المسارات وخدمة لوحة التحكم.
- **Telethon**: مكتبة تليجرام المتقدمة عبر بروتوكول MTProto (تتيح التحكم بحسابات المستخدمين Userbots دون الحاجة لـ Bot API المحدود).
- **Cryptography (Fernet)**: تشفير جلسات تليجرام بمفتاح تشفير متماثل AES-256.
- **PyMongo & dnspython**: للاتصال السحابي بقواعد بيانات MongoDB Atlas عبر بروتوكول `mongodb+srv://`.
- **Werkzeug**: لتشفير كلمات المرور عبر خوارزميات `scrypt` و `pbkdf2:sha256`.
- **Requests**: للاتصال ببوابة الدفع NOWPayments وإرسال إشعارات الـ Webhooks.
- **Python-Dotenv**: لقراءة وإدارة متغيرات البيئة السرية `.env`.

### ج. الخدمات الخارجية المدمجة (Third-Party Integrations)
- **NOWPayments API**: بوابة دفع بالعملات المشفرة تدعم USDT (TRC20, ERC20, BEP20), BTC, ETH, TON, SOL, LTC مع معالجة إشعارات الـ IPN فوراً.
- **SMTP Mail Server**: إرسال رسائل تأكيد الحساب وتوليد رموز الـ OTP عبر أي مزود بريد إلكتروني (Gmail, Brevo, SendGrid, Mailgun).

---

## 4. المحركات والأنظمة الفرعية الأساسية

### 1. محرك التوجيه اللحظي (`ForwarderEngine` & `WorkerPool`)
- **عزل المستخدمين (Multi-Tenant Worker Isolation)**: يمتلك كل مستخدم مشترك Worker خاص به يعمل في مسار خيطي منفصل يعالج رسائل قنواته فقط دون التداخل مع غيره.
- **معالج الألبومات (`AlbumCollector`)**: يقوم بتجميع الصور ومقاطع الفيديو المتعددة المرسلة في نفس المنشور وإعادة إرسالها كألبوم واحد متكامل مع الحفاظ على النص التوضيحي (Caption).
- **التوجيه متعدد الوجهات (Multi-Target Forwarding)**: إمكانية توجيه الرسالة الواحدة من قناة مصدر إلى عدة قنوات هدف في نفس اللحظة.
- **فلترة نوع الوسائط (Media Filtering)**: خيارات لاختيار أو استثناء أنواع الوسائط (صور، فيديو، مستندات، مقاطع صوتية، ملصقات، صور متحركة GIF).

### 2. محرك القواعد وتحويل النصوص (`RulesEngine`)
- **استبدال التعابير النمطية (Regex Replacement)**: لتغيير الروابط، أرقام الهواتف، الأسعار، أو الكلمات بدقة بالغة.
- **حذف المعرفات والروابط (`Strip Mentions & Links`)**: تنظيف النصوص من الروابط الخارجية ومعرفات `@channel`.
- **القائمة البيضاء والسوداء (Keywords Whitelist / Blacklist)**: توجيه الرسائل التي تحتوي على كلمات محددة فقط أو حظر الرسائل التي تحتوي على كلمات محظورة.
- **الترويسة والتذييل المخصص (Header & Footer Templates)**: إضافة نصوص وروابط تسويقية ثابتة قبل أو بعد كل رسالة موجهة.

### 3. نظام الاشتراكات والفوترة المشفرة (`BillingEngine`)
- تجربة مجانية تلقائية لمدة **3 أيام** لجميع المشتركين الجدد.
- خطط متعددة: **شهري (Monthly)**، **ربع سنوي (Quarterly)**، **سنوي VIP (Annual)**.
- توليد فواتير دفع مشفرة مباشرة وربطها بمعرف المستخدم، مع التفعيل الآلي بنسبة 100% بمجرد تأكيد البلوكشين عبر الـ Webhook.
- نظام توليد واسترداد **مفاتيح التراخيص اليدوية (License Keys)** للإدارة.

### 4. نظام تأكيد البريد الإلكتروني (`EmailService`)
- إرسال بريد ترحيبي وتأكيدي فوري عند التسجيل.
- تفعيل برابط مباشر بضغطة واحدة (`/verify-email?token=...`) أو برمز تحقق من 6 أرقام (OTP).
- مؤقت تنازلي ذكي لمنع السبام وإعادة الإرسال.

### 5. نظام الربط المباشر لتليجرام (`TelegramWebAuth`)
- واجهة ويب تتيح للمستخدم إدخال رقم هاتفه، واستقبال كود تليجرام في التطبيق، وتأكيد كلمة مرور التحقق بخطوتين (2FA) دون أي أوامر برمجية أو شاشات سوداء.
- حفظ الجلسة بصيغة `StringSession` مشفرة ومحمية بـ AES-256.

---

## 5. سجل المشاكل والتحديات التي تمت مواجهتها وكيف تم حلها

| # | المشكلة / التحدي | السبب التقني | الحل المطبق والنتيجة |
|---|---|---|---|
| 1 | **حظر التوجيه من القنوات المقيدة (Restricted Channels)** | تقوم بعض القنوات بحظر إعادة التوجيه التلقائي (Forwarding Restricted) لمنع نسخ المحتوى. | تم ابتكار حل **In-Memory Download & Re-upload Fallback** في `engine.py` حيث يقوم البوت بتحميل الوسائط مؤقتاً في الذاكرة وإعادة رفعها كمنشور جديد دون ترك أي أثر توجيه. |
| 2 | **تفتت ألبومات الصور والفيديو (Media Group Fragmentation)** | يرسل تليجرام كل صورة في الألبوم كرسالة منفصلة بفارق ميلي ثانية. | تم بناء **`AlbumCollector`** مع نافذة تجميع ذكية (Debounce Window 1.5s) لجمع كل عناصر المنشور وإرسالها كحزمة `SendMediaGroup` واحدة. |
| 3 | **أخطاء التوقف المؤقت من تليجرام (FloodWaitError)** | إرسال رسائل متتالية بسرعة عالية يؤدي إلى تقييد تليجرام المؤقت للحساب. | تم إنشاء نظام **Anti-Flood Waiting Queue** يقوم بحساب وقت الانتظار بدقة، وتجميد القناة المحددة فقط مؤقتاً مع إضافة 5 ثوانٍ كأمان، ثم متابعة التوجيه تلقائياً دون إيقاف باقي القنوات. |
| 4 | **توافق قواعد البيانات في البيئات المختلفة** | في بيئات التطوير المحلية قد لا يتوفر رابط MongoDB Atlas السحابي. | تم تصميم نظام **`DatabaseFallback`** الثنائي الذي يعمل بـ SQLite محلياً بنفس دوال وواجهات MongoDB (`find`, `update_one`, `insert_one`, `count_documents`) مع الترحيل التلقائي للأعمدة (Migrations). |
| 5 | **محاكاة الدفع الوهمية السابقة** | كانت بوابة الدفع السابقة عبارة عن نافذة محاكاة غير حقيقية. | تم ربط المنصة رسمياً ببوابة **NOWPayments** الحقيقية بمفاتيح API و IPN حية، وتصميم شاشة كروت العملات المشفرة (USDT, BTC, ETH, TON, SOL) والتفعيل التلقائي الفوري. |
| 6 | **صلاحيات الإدارة ولوحة المفاتيح المخفية** | الحاجة لفصل مستخدمي النظام ومنح السوبر أدمن صلاحيات كاملة وتوليد مفاتيح. | تم اعتماد `alooshpal@gmail.com` كسوبر أدمن دائم، وبناء نظام ترخيص وتجميد حسابات، وإخفاء لوحة الإدارة تماماً عن باقي العملاء وحمايتها برمجياً على مستوى الـ API والـ UI. |
| 7 | **ازدحام الترويسة العلوية وتشتت الأزرار** | تراكم الأزرار والتبويبات المزدوجة في شريط التنقل العلوي للوحة التحكم. | تمت إعادة هيكلة الـ Navbar، ونقل زر **"الخطط والاشتراكات"** و **"لوحة الإدارة"** للأعلى بجوار الشعار مباشرة، وتصفية التبويبات لـ 6 أدوات تشغيلية رئيسية واضحة. |
| 8 | **توحيد البراند والهوية البصرية** | وجود مسميات قديمة متفرقة للبرنامج في ملفات النظام. | تم استبدال جميع المسميات إلى العلامة الرسمية الموحدة **TeleTips Pro** في الواجهات، السجلات، رسائل البريد، تذييل الرسائل، والتوثيق. |
| 9 | **تأكيد البريد الإلكتروني عند التسجيل** | كان الحساب ينشأ مباشرة دون التحقق من ملكية البريد. | تم بناء نظام **Email Verification Engine** مع خادم SMTP، وقوالب HTML داكنة احترافية، ورموز OTP من 6 أرقام مع رابط تفعيل مباشر واختبارات شاملة. |

---

## 6. النقاط المفتوحة والمتطلبات التشغيلية
*(ما يحتاج تهيئة من طرف المستخدم أو يوصى به مستقبلاً)*

1. **ضبط بيانات الـ SMTP للبريد الفعلي في بيئة Render**:
   - محرك البريد الإلكتروني جاهز ومبرمج بالكامل. لكي تصل الرسائل الفعلية لصندوق وارد العملاء، يجب على مدير النظام إضافة متغيرات البيئة في لوحة تحكم Render:
     - `SMTP_HOST` (مثلاً: `smtp.gmail.com` أو `smtp-relay.brevo.com`)
     - `SMTP_PORT` (مثلاً: `587`)
     - `SMTP_USER` (البريد المرسل)
     - `SMTP_PASSWORD` (كلمة مرور التطبيق App Password)
     - `SMTP_TLS` (`true`)
   - *(في حال عدم إضافتها، يقوم النظام تلقائياً بتسجيل رمز الـ OTP ورابط التفعيل في السجلات logs دون تعطيل أي مستخدم)*.

2. **بيانات تليجرام المخصصة (API_ID & API_HASH)**:
   - التطبيق مزود بقيم افتراضية جاهزة لتليجرام، ولكن لأعلى درجات الأداء والاستقرار ينصح باستخدام `API_ID` و `API_HASH` رسمية من [my.telegram.org](https://my.telegram.org).

3. **التوسع الأفقي للآلاف من القنوات (Scaling Recommendation)**:
   - النظام الحالي يدير مئات القنوات بكفاءة تامة عبر الـ Multi-Threading Worker Pool. عند التوسع لعشرات الآلاف من المستخدمين المتزامنين، يوصى بربط Redis و Celery لتوزيع أحمال الـ Workers على عدة خوادم.

---

## 7. هيكل قواعد البيانات ونموذج البيانات

### جدول / مجموعة المستخدمين (`users` Collection)
- `_id`: معرف فريد (UUID).
- `email`: البريد الإلكتروني (فريد ومفهرس).
- `name`: اسم المشترك.
- `password_hash`: كلمة المرور المشفرة (Scrypt).
- `plan`: الخطة الحالية (`trial`, `monthly`, `quarterly`, `annual`).
- `role`: الصلاحية (`super_admin`, `admin`, `client`).
- `subscription_status`: حالة الاشتراك (`active`, `trial`, `expired`).
- `subscription_expires_at`: تاريخ انتهاء الاشتراك.
- `max_target_channels`: الحد الأقصى للقنوات المسموح بتوجيهها.
- `is_verified`: حالة تأكيد البريد الإلكتروني (`True` / `False`).
- `verification_token`: رمز تفعيل الرابط المشفر.
- `verification_otp`: رمز الـ OTP المكون من 6 أرقام.
- `verification_expires_at`: تاريخ انتهاء صلاحية رمز التحقق (24 ساعة).
- `telegram_account`: بيانات حساب تليجرام المتصل (مع جلسة مشفرة).
- `is_frozen`: حالة تجميد الحساب من قبل الإدارة.
- `frozen_reason`: سبب التجميد.

### جدول / مجموعة قواعد التوجيه (`forwarding_rules` Collection)
- `_id`: معرف القاعدة.
- `user_id`: معرف المستخدم المالك.
- `source_id`: معرف أو يوزر القناة المصدر.
- `source_title`: اسم القناة المصدر.
- `target_ids`: قائمة بمعرفات القنوات الهدف.
- `target_titles`: أسماء القنوات الهدف.
- `is_active`: حالة تفعيل القاعدة.
- `forward_mode`: نمط التوجيه (`direct` توجيه أصلي، `custom_content` إعادة إرسال بمحتوى معدل).
- `forward_delay`: تأخير زمني بالثواني قبل التوجيه.
- `media_types`: قائمة بأنواع الوسائط المسموحة.
- `whitelist_keywords`: الكلمات المفتاحية المقبولة.
- `blacklist_keywords`: الكلمات المفتاحية المرفوضة.
- `strip_mentions`: إزالة المعرفات `@username`.
- `strip_links`: إزالة الروابط `https://...`.
- `header_template`: نص الترويسة المضافة.
- `footer_template`: نص التذييل المضاف (الافتراضي: `Forwarded by TeleTips Pro`).

### جدول / مجموعة مفاتيح التراخيص (`license_keys` Collection)
- `key_code`: كود المفتاح (مثال: `TT-ANNUAL-XXXX-XXXX`).
- `plan_id`: الخطة المرتبطة (`monthly`, `quarterly`, `annual`).
- `duration_days`: عدد أيام التفعيل (30، 90، 365).
- `is_redeemed`: هل تم استخدام المفتاح.
- `redeemed_by`: المستخدم الذي قام بالتفعيل.
- `created_by`: الأدمن الذي قام بإنشاء المفتاح.

---

## 8. الأمان والتشفير وإدارة الصلاحيات

1. **تشفير الجلسات (Telegram Session Encryption)**:
   - جلسات تليجرام (`StringSession`) لا تخزن كنصوص واضحة مطلقاً، بل تشفر بـ **AES-256 Fernet** ولا تفك إلا لحظة تشغيل الـ Worker في الذاكرة المؤقتة.
2. **أمان المصادقة (Auth Tokens & Cookies)**:
   - استخدام الـ HMAC-SHA256 لتوقيع التوكنات مع تاريخ صلاحية زمني، وحمايتها بخاصية `HttpOnly` و `SameSite=Lax` لمنع هجمات XSS و CSRF.
3. **عزل البيانات الصارم (Multi-Tenant Isolation)**:
   - لا يمكن لأي مستخدم الوصول لقواعد أو قنوات أو سجلات مستخدم آخر؛ حيث يتم استخراج `g.current_user` وفلترة جميع استعلامات قاعدة البيانات بناءً على `user_id`.

---

## 9. خاتمة وتوصيات التطوير المستقبلي

تم إنجاز المشروع وفق أعلى معايير الجودة البرمجية، حيث اجتاز التطبيق بالكامل **108 اختباراً برمجياً آلياً (108 / 108 Automated Tests Passed بنسبة 100%)**، مع رفع ونشر كافة التحديثات إلى مستودع الإنتاج الرئيسي على GitHub.

### توصيات مستقبلية اختيارية:
- إضافة روبوت إشعارات تليجرام (Telegram Notification Bot) يرسل تنبيهات للمستخدم عند توقف حسابه أو قرب انتهاء اشتراكه.
- دعم الذكاء الاصطناعي (AI Rewrite Engine) لإعادة صياغة المنشورات وتلخيصها تلقائياً قبل إعادة التوجيه.

---
**TeleTips Pro © 2026 — All Rights Reserved**
